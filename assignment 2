# 1. Create variables to store your name, age, and whether you're a student (bool)
name = "Udayraj"
age = 20
is_student = True

# 2. Print out the type of each variable
print("Type of name:", type(name))
print("Type of age:", type(age))
print("Type of is_student:", type(is_student))

# 3. Convert a string number (e.g., "45") to an integer and add 10
string_number = "45"
converted_number = int(string_number)
result = converted_number + 10
print("Result after adding 10:", result)

# 4. Try converting a float to an integer and print the result
float_number = 3.9
converted_float = int(float_number)
print("Converted float to int:", converted_float)

# 5. Use type() to check the type of a variable after type conversion
print("Type after converting string to int:", type(converted_number))
print("Type after converting float to int:", type(converted_float))
